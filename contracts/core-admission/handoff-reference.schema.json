{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:studious-lamp:core-admission:0.2.0:handoff-reference",
  "title": "Studious Core Admission 0.2.0 Handoff Reference",
  "description": "Owner-selected control-plane schema, separate from all four Candidate-content schemas. Exactly eleven mandatory fields plus exactly three mandatory remote-precondition fields. This shape is neither authority nor proof of bytes, release, immutable version or admission. No credential, principal, binding, path, mutable selector, private profile or raw provenance is permitted. Only public opaque identities and digest evidence belong here.",
  "$comment": "Selection only: core-admission / 0.2.0 / core-admission-v0.2.0. Complete independent release inventory is in openapi.yaml x-release. No tag, release candidate, publication, remote locator or runtime is established. Digest pins refer to exact released bytes verified independently; this schema MUST NOT embed its own recursive hash. Raw-source digests used by T010 are local test substitutions only.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "handoff_reference_contract_version",
    "handoff_reference_contract_sha256",
    "environment_id",
    "space_id",
    "manifest_resource_id",
    "remote_manifest_precondition",
    "candidate_id",
    "candidate_contract_release_id",
    "candidate_contract_release_manifest_sha256",
    "candidate_manifest_sha256",
    "export_receipt_id"
  ],
  "properties": {
    "handoff_reference_contract_version": {
      "type": "string",
      "const": "0.2.0"
    },
    "handoff_reference_contract_sha256": {
      "$ref": "#/$defs/Sha256",
      "description": "SHA-256 of exact independently pinned released Handoff schema bytes. Core compares with its verified release inventory before intake. Shape alone cannot verify this value; no recursive self-hash constant."
    },
    "environment_id": {
      "$ref": "#/$defs/StableId"
    },
    "space_id": {
      "$ref": "#/$defs/StableId"
    },
    "manifest_resource_id": {
      "$ref": "#/$defs/StableId"
    },
    "remote_manifest_precondition": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "version_id",
        "readback_size_bytes",
        "readback_sha256"
      ],
      "properties": {
        "version_id": {
          "$ref": "#/$defs/StrongVersionId"
        },
        "readback_size_bytes": {
          "type": "integer",
          "minimum": 1,
          "maximum": 16777216
        },
        "readback_sha256": {
          "$ref": "#/$defs/Sha256"
        }
      },
      "description": "All three values required. Producer readback size is exact manifest bytes (1..16 MiB); readback_sha256 MUST equal candidate_manifest_sha256. Core must independently recheck size/digest and strong version on both conditional manifest reads. The adapter must map version_id losslessly to one immutable supported version/strong validator; weak ETag, wildcard, timestamp, sync, upload or ACL evidence cannot substitute. If exact conditional retrieval is unavailable, reject without unconditional fallback."
    },
    "candidate_id": {
      "$ref": "#/$defs/StableId"
    },
    "candidate_contract_release_id": {
      "$ref": "#/$defs/StableId",
      "description": "Exact immutable Candidate-content release identity independently pinned with its manifest digest. No Core release inference, range, alias or mutable lookup."
    },
    "candidate_contract_release_manifest_sha256": {
      "$ref": "#/$defs/Sha256"
    },
    "candidate_manifest_sha256": {
      "$ref": "#/$defs/Sha256"
    },
    "export_receipt_id": {
      "$ref": "#/$defs/StableId"
    }
  },
  "$defs": {
    "StableId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}(?![\\s\\S])",
      "not": {
        "anyOf": [
          {
            "pattern": "^(?:[Ll][Aa][Tt][Ee][Ss][Tt]|[Mm][Aa][Ii][Nn]|[Mm][Aa][Ss][Tt][Ee][Rr]|[Hh][Ee][Aa][Dd]|[Cc][Uu][Rr][Rr][Ee][Nn][Tt]|[Ss][Tt][Aa][Bb][Ll][Ee]|[Dd][Ee][Ff][Aa][Uu][Ll][Tt])(?![\\s\\S])"
          },
          {
            "pattern": "\\.\\."
          }
        ]
      },
      "description": "Opaque, case-sensitive stable identity; never a path, URL, principal, secret or authority. No decoding, trimming, case folding, alias or mutable-selector resolution. Issuer stability and resource ownership require runtime verification."
    },
    "Sha256": {
      "type": "string",
      "minLength": 64,
      "maxLength": 64,
      "pattern": "^[0-9a-f]{64}(?![\\s\\S])"
    },
    "StrongVersionId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}(?![\\s\\S])",
      "not": {
        "anyOf": [
          {
            "pattern": "^(?:[Ll][Aa][Tt][Ee][Ss][Tt]|[Mm][Aa][Ii][Nn]|[Mm][Aa][Ss][Tt][Ee][Rr]|[Hh][Ee][Aa][Dd]|[Cc][Uu][Rr][Rr][Ee][Nn][Tt]|[Ss][Tt][Aa][Bb][Ll][Ee]|[Dd][Ee][Ff][Aa][Uu][Ll][Tt])(?![\\s\\S])"
          },
          {
            "pattern": "\\.\\."
          },
          {
            "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}"
          }
        ]
      },
      "description": "Opaque stable version token, not a weak validator, timestamp or mutable selector. A syntactically valid token is not proof of strength; verify the supported remote conditional-read semantics independently. Never strip W/ or transform an unsupported remote version to make it fit."
    }
  }
}
